const state = {
    stages: [
        'New Candidates',
        'Waiting for Reply',
        'Screening (recruiter)',
    ]
}

const root = document.getElementById('crm') as HTMLElement | undefined
console.log("root: ", root);